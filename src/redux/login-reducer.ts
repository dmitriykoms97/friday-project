const initialState = {}

export const loginReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        case '':
            return state
        default:
            return state
    }
}