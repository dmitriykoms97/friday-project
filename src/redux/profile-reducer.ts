const initialState = {}

export const profileReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        case '':
            return state
        default:
            return state
    }
}