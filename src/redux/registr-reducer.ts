const initialState = {}

export const registrReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        case '':
            return state
        default:
            return state
    }
}